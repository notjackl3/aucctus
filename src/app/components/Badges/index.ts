import Simple from './SimpleBadge';
import RiskLevel from './RiskLevelBadge';
import Assumption from './AssumptionBadge/AssumptionBadge';

const Badges = {
  Simple,
  RiskLevel,
  Assumption,
};

export default Badges;
