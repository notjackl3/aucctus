# Aucctus Development Rules

## React TypeScript Guidelines

### Components
- Use functional components with hooks instead of class components
- Use arrow functions for component definitions
- Use destructuring for props
- Implement lazy loading and code splitting
- Optimize images and assets
- Use memoization for expensive calculations
- Avoid unnecessary re-renders using useMemo and useCallback

### Security
- Sanitize user input
- Avoid storing sensitive information in local storage
- Use environment variables for sensitive configuration

### Documentation
- Document complex logic with comments
- Document component props with JSDoc
- Document custom hooks with JSDoc

## Styling Guidelines

### Theme System
- Always use Aucctus theme classes as the primary styling approach
- Theme classes are organized into four main categories:
  - Background: `aucctus-bg-*` for consistent background colors
  - Text: `aucctus-text-*` for typography colors
  - Border: `aucctus-border-*` for border colors
  - Typography: `aucctus-header-*` and `aucctus-text-*` for text styles

#### Background Colors
- Use semantic background classes:
  - `aucctus-bg-primary` for main surfaces
  - `aucctus-bg-secondary` for secondary surfaces
  - `aucctus-bg-brand-*` for branded elements
  - `aucctus-bg-error/warning/success-*` for status indicators
- Add `-hover` suffix for hover states
- Use `-alt` and `-subtle` variants where appropriate

#### Typography
- Use semantic text color classes:
  - `aucctus-text-primary` for main content
  - `aucctus-text-secondary` for supporting text
  - `aucctus-text-brand-*` for branded text
  - `aucctus-text-error/warning/success-*` for status text
- Apply typography scales consistently:
  - Headers: `aucctus-header-{2xl|xl|lg|md|sm|xs}`
  - Body text: `aucctus-text-{xl|lg|md|sm|xs}`
  - Use weight modifiers: `-medium`, `-semibold`, `-bold`

#### Borders
- Use semantic border classes:
  - `aucctus-border-primary` for main borders
  - `aucctus-border-brand` for branded elements
  - `aucctus-border-error` for error states

### Tailwind Best Practices
- Use Aucctus theme classes first, then supplement with Tailwind utilities
- Implement responsive utilities for different screen sizes
- Use animation utilities for transitions
- ALWAYS use the `cn` utility with object notation for conditional classes

### Component Design
- Ensure responsive design using Tailwind's responsive prefixes
- Maintain accessibility with proper color contrast
- Use semantic HTML elements
- Provide appropriate ARIA attributes
- Support keyboard navigation
- Follow established component patterns for consistency

### Code Quality
- Follow ESLint and Prettier configurations
- Optimize for performance
- Ensure component accessibility
- Be vigilant about type safety and type inference
